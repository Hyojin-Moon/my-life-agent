{
  "name": "@my-life-agent/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun run --env-file .env --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun run dist/index.js",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@my-life-agent/ai-agent": "workspace:*",
    "@my-life-agent/database": "workspace:*",
    "@my-life-agent/shared": "workspace:*",
    "@hono/zod-validator": "^0.4.0",
    "hono": "^4.6.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.7.0"
  }
}
